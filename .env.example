# Node environment (development/production)
NODE_ENV=production

# Server Configuration
PORT=5001
MONGODB_URI=mongodb://${MONGO_ROOT_USERNAME}:${MONGO_ROOT_PASSWORD}@mongodb:27017/runwatch?authSource=admin

# MongoDB Credentials (required — used by docker-compose)
MONGO_ROOT_USERNAME=runwatch_admin
MONGO_ROOT_PASSWORD=change_me_to_a_strong_password

# GitHub Configuration
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret
GITHUB_APP_ID=your_github_app_id
GITHUB_APP_PRIVATE_KEY_PATH=path_to_your_private_key.pem

# Client Configuration
CLIENT_URL=http://localhost
REACT_APP_API_URL=http://localhost/api
REACT_APP_WEBSOCKET_URL=ws://localhost