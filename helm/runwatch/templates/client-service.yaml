apiVersion: v1
kind: Service
metadata:
  name: {{ include "runwatch.client.fullname" . }}
  labels:
    {{- include "runwatch.client.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "runwatch.client.selectorLabels" . | nindent 4 }}
