# Node environment (development/production)
NODE_ENV=production

# Server Configuration
PORT=5001
# NOTE: dotenv does not perform variable substitution. Set credentials above and update this URI to a literal string.
# The app connects as MONGO_APP_USERNAME (not the root admin) — principle of least privilege.
MONGODB_URI=mongodb://runwatch_app:change_me_to_a_strong_app_password@mongodb:27017/runwatch?authSource=runwatch

# MongoDB Root Credentials (required — used by docker-compose to initialise MongoDB)
MONGO_ROOT_USERNAME=runwatch_admin
MONGO_ROOT_PASSWORD=change_me_to_a_strong_password

# MongoDB App Credentials (least-privilege user — used by the Node.js application)
# Created automatically by mongo-init.js on first container startup.
MONGO_APP_USERNAME=runwatch_app
MONGO_APP_PASSWORD=change_me_to_a_strong_app_password

# Admin API token — required for /api/database/backup and /api/database/restore.
# Generate with: openssl rand -hex 32
ADMIN_API_TOKEN=change_me_to_a_long_random_secret

# GitHub Configuration
GITHUB_WEBHOOK_SECRET=your_github_webhook_secret
GITHUB_APP_ID=your_github_app_id
GITHUB_APP_PRIVATE_KEY_PATH=path_to_your_private_key.pem

# Client Configuration
CLIENT_URL=http://localhost
REACT_APP_API_URL=http://localhost/api
REACT_APP_WEBSOCKET_URL=ws://localhost